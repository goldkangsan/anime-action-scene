# 3d/requirements.txt
# Champ inference(생성) 전용 의존성
#
# Champ 원본 requirements.txt 에서 세 가지만 손봤다:
#   1. torch/torchvision 은 여기서 설치하지 않는다 (아래 참고). RunPod PyTorch 이미지에
#      CUDA 빌드가 이미 있고, CPU torch 를 핀으로 박으면 GPU 추론이 깨진다.
#   2. 학습 전용(tensorboard) 제거.
#   3. huggingface_hub(download_weights.py) 와 pyyaml 추가.
#
# ---------------------------------------------------------------------------------------
# TORCH: 박스에 맞는 CUDA 빌드를 이 파일보다 먼저 설치할 것. Champ 원본은 torch 2.2.2 /
# torchvision 0.17.2 기준. RunPod "PyTorch" 템플릿이면 이미 깔려 있다.
# 수동 설치 예 (CUDA 11.8):
#     pip install torch==2.2.2 torchvision==0.17.2 --index-url https://download.pytorch.org/whl/cu118
# 주의: xformers 는 torch 버전에 민감하다. 이미지의 torch 와 안 맞으면 버전을 바꾸거나 빼라
#       (Champ 는 xformers 없이도 동작, VRAM 만 더 씀 → config 의 enable_xformers=false).
# ---------------------------------------------------------------------------------------

accelerate==0.21.0
controlnet-aux==0.0.7
diffusers==0.24.0
einops==0.4.1
huggingface_hub>=0.19.4
imageio==2.33.0
imageio[ffmpeg]
matplotlib==3.8.3
numpy==1.23.5
omegaconf==2.2.3
Pillow==9.5.0
PyYAML>=6.0
safetensors==0.4.2
onnxruntime-gpu==1.13.1
open-clip-torch==2.20.0
opencv-contrib-python-headless==4.8.1.78
opencv-python-headless==4.8.1.78
torchmetrics==1.2.1
tqdm==4.66.1
transformers==4.30.2
# xformers==0.0.25.post1   # torch 2.2.2 전용 — 이미지 torch 에 맞춰서 켜라

# ---------------------------------------------------------------------------------------
# 아래는 raw 영상 → guidance "전처리"에만 필요하다. 생성만 할 거면 설치하지 마라.
# 전처리는 별도의 무거운 환경이 필요하다 (docs/data_process.md, preprocess.py):
#   - 4D-Humans, detectron2 (SMPL 추정)      : pip install git+https://github.com/shubham-goel/4D-Humans 등
#   - Blender 3.6                            : 조건맵 렌더 (pip 아님, 별도 설치)
#   - pyrender==0.1.45, ultralytics==8.1.34  : 렌더 / DWPose 보조
#   - SMPL body model (.pkl)                 : 라이선스 필요, smpl.is.tue.mpg.de 에서 수동 다운로드
#   - DWPose 리포                            : git clone https://github.com/IDEA-Research/DWPose
# ---------------------------------------------------------------------------------------
